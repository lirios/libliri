liri_add_qml_plugin(CoreQmlPlugin
    MODULE_PATH
        "Liri/Core"
    VERSION
        "1.0"
    OUTPUT_NAME
        "liricoreplugin"
    SOURCES
        formatter.cpp
        formatter.h
        plugin.cpp
    QML_FILES
        qmldir
    DEFINES
        QT_NO_CAST_FROM_ASCII
        QT_NO_FOREACH
    PUBLIC_LIBRARIES
        Liri::Core
)

liri_finalize_qml_plugin(CoreQmlPlugin)
