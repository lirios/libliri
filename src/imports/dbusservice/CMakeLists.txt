liri_add_qml_plugin(liridbusservice
    MODULE_PATH
        "Liri/DBusService"
    VERSION
        "1.0"
    SOURCES
        application.cpp
        application.h
        plugin.cpp
    QML_FILES
        qmldir
    DEFINES
        QT_NO_CAST_FROM_ASCII
        QT_NO_FOREACH
    PUBLIC_LIBRARIES
        Qt5::DBus
)

liri_finalize_qml_plugin(liridbusservice)
